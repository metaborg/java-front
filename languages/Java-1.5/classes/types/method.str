module languages/Java-1.5/classes/types/method

imports
	
	include/Java
	lib/types/-
	
rules
	
	create-type-task(|ctx):
		MethodParams(p*) -> [ty*]
		where
			ty* := <map(type-task(|ctx))> p*
			
	// TODO: unnecessary since type is stored on name using NaBL?
	create-type-task(|ctx):
		Param(_, t, _) -> <type-task(|ctx)> t