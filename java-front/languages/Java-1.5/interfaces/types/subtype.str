module languages/Java-1.5/interfaces/types/subtype

imports
	
	include/Java
	languages/Java-1.5/types/types/subtype

rules
	
	nabl-constraint(|ctx):
		InterfaceDec(InterfaceDecHead(_, i, _, extends*), _) -> <fail>
		where
			<is-list> extends*
		with
			<map(\ExtendsInterfaces(ei) -> <where(store-subinterface-relation(|ctx, i)); store-widening-relation(|ctx, i)> ei\)> extends*
