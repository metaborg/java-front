module refret/unit-tests/paper-examples

test enclosing [[
test;

[Outer]
class Outer {
    int [[x]] = 1;
    Inner [[i]];

    public class Inner {
        int bar() {
            return x;
        }
    }

    int foo() {
        return [[x]];
    }
}
]]
run fix-reference(|#3, #1, #2) fails