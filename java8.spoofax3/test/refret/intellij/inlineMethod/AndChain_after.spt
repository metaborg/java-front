module refret/intellij/inlineMethod/AndChain_after

test AndChain_after: parsing [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]] parse succeeds

test AndChain_after: default analysis [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]] analysis succeeds

test AndChain_after: test analysis [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]] run test-analyze to SUCCEED()

test AndChain_after: refret test 1 [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean [[isEmpty]]() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if ([[isEmpty]]())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]] run fix-reference(|#2, #1) to [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]]

test AndChain_after: refret test 2 [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] [[items]] = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return [[items]][ head ] > x;
      }
  
      // other methods
    }
]] run fix-reference(|#2, #1) to [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]]

test AndChain_after: refret test 3 [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int [[head]];
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ [[head]] ] > x;
      }
  
      // other methods
    }
]] run fix-reference(|#2, #1) to [[
  test;
  [MyQueue]
      import java.util.NoSuchElementException;
  
    public class MyQueue {
      private long[] items = new long[16];
  
      private int head;
      private int tail;
  
      public boolean isEmpty() { return head == tail; }
  
        // true if the first element of the queue is greater than specified number
      public boolean firstIsGreaterThan(int x) {
          if (isEmpty()) return false;
          if (isEmpty())
            throw new NoSuchElementException("Queue is empty");
          return items[ head ] > x;
      }
  
      // other methods
    }
]]

