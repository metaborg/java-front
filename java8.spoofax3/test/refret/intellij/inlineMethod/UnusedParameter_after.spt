module refret/intellij/inlineMethod/UnusedParameter_after

test parse: UnusedParameter_after [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object size();
      }
  
      public Object foo(Set bar) {
        if (bar.size() < 2) {// Inline this    // TODO: Context
            bar.size(); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]] parse succeeds

test analysis: UnusedParameter_after [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object size();
      }
  
      public Object foo(Set bar) {
        if (bar.size() < 2) {// Inline this    // TODO: Context
            bar.size(); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]] run test-analyze to SUCCEED()

test refret 0: UnusedParameter_after [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object [[size]]();
      }
  
      public Object foo(Set bar) {
        if ([[size]]() < 2) {// Inline this    // TODO: Context
            bar.size(); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]] run fix-reference(|#2, #1) to [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object size();
      }
  
      public Object foo(Set bar) {
        if (bar.size() < 2) {// Inline this    // TODO: Context
            bar.size(); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]]

test refret 1: UnusedParameter_after [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object [[size]]();
      }
  
      public Object foo(Set bar) {
        if (bar.size() < 2) {// Inline this    // TODO: Context
            [[size]](); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]] run fix-reference(|#2, #1) to [[
  test;
  [Test]
      class Temp {
      class Set {
        native Object size();
      }
  
      public Object foo(Set bar) {
        if (bar.size() < 2) {// Inline this    // TODO: Context
            bar.size(); // or inline this    // TODO: Context
            return null;
        }
  
        return bar;
      }
  
    }
]]

