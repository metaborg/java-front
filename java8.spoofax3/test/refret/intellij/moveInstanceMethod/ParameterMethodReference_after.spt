module refret/intellij/moveInstanceMethod/ParameterMethodReference_after

/*
test ParameterMethodReference_after: parsing [[
  [[{disabled}]] // Method reference
  
  test;
  [Test]
      import java.util.stream.Stream;
  
    class Test {
      private static class Destination{
          private boolean notNull() {
            return this != null;
          }
      }
  
      private final Destination destination = new Destination();
  
      public void main(Stream<Destination> stream){
        stream.filter(Destination::notNull);
      }
  
    }
]] parse succeeds
*/

/*
test ParameterMethodReference_after: default analysis [[
  [[{disabled}]] // Method reference
  
  test;
  [Test]
      import java.util.stream.Stream;
  
    class Test {
      private static class Destination{
          private boolean notNull() {
            return this != null;
          }
      }
  
      private final Destination destination = new Destination();
  
      public void main(Stream<Destination> stream){
        stream.filter(Destination::notNull);
      }
  
    }
]] analysis succeeds
*/

/*
test ParameterMethodReference_after: test analysis [[
  [[{disabled}]] // Method reference
  
  test;
  [Test]
      import java.util.stream.Stream;
  
    class Test {
      private static class Destination{
          private boolean notNull() {
            return this != null;
          }
      }
  
      private final Destination destination = new Destination();
  
      public void main(Stream<Destination> stream){
        stream.filter(Destination::notNull);
      }
  
    }
]] run test-analyze to SUCCEED()
*/

/*
test ParameterMethodReference_after: refret test 1 [[
  [[{disabled}]] // Method reference
  
  test;
  [Test]
      import java.util.stream.Stream;
  
    class Test {
      private static class Destination{
          private boolean [[notNull]]() {
            return this != null;
          }
      }
  
      private final Destination destination = new Destination();
  
      public void main(Stream<Destination> stream){
        stream.filter([[notNull]]);
      }
  
    }
]] run fix-reference(|#2, #1) to [[
  [[{disabled}]] // Method reference
  
  test;
  [Test]
      import java.util.stream.Stream;
  
    class Test {
      private static class Destination{
          private boolean notNull() {
            return this != null;
          }
      }
  
      private final Destination destination = new Destination();
  
      public void main(Stream<Destination> stream){
        stream.filter(Destination::notNull);
      }
  
    }
]]
*/

