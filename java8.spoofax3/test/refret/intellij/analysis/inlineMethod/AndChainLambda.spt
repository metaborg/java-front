module refret/intellij/analysis/inlineMethod/AndChainLambda

/*
test AndChainLambda: default analysis [[
  // Generics
  test;
  [Test]
      import java.util.function.Predicate;
  
    class X {
      Predicate<String> predicate() {
        return s -> {
            if (s.isEmpty()) return false;
            int length = s.length();
            int ch = s.charAt(0);
            return length + ch > 0;
        };
      }
  
    }
]] analysis succeeds
*/

/*
test AndChainLambda: test analysis [[
  // Generics
  test;
  [Test]
      import java.util.function.Predicate;
  
    class X {
      Predicate<String> predicate() {
        return s -> {
            if (s.isEmpty()) return false;
            int length = s.length();
            int ch = s.charAt(0);
            return length + ch > 0;
        };
      }
  
    }
]] run test-analyze to SUCCEED()
*/

