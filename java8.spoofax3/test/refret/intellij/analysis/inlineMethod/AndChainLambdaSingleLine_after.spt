module refret/intellij/analysis/inlineMethod/AndChainLambdaSingleLine_after

/*
test AndChainLambdaSingleLine_after: default analysis [[
  [[{disabled}]] // Generics
  test;
  [Test]
      import java.util.function.Predicate;
  
    class X {
      Predicate<String> predicate() {
        return s -> !s.isEmpty() && s.length() + s.charAt(0) > 0;
      }
  
    }
]] analysis succeeds
*/

/*
test AndChainLambdaSingleLine_after: test analysis [[
  [[{disabled}]] // Generics
  test;
  [Test]
      import java.util.function.Predicate;
  
    class X {
      Predicate<String> predicate() {
        return s -> !s.isEmpty() && s.length() + s.charAt(0) > 0;
      }
  
    }
]] run test-analyze to SUCCEED()
*/

