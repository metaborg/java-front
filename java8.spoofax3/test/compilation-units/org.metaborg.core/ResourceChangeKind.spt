module ResourceChangeKind

language java8

start symbol Start

test ResourceChangeKind [[ package org.metaborg.core.resource;

public enum ResourceChangeKind {
    Create, Delete, Rename, Copy, Modify,
} ]] parse succeeds
