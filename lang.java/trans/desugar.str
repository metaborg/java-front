module desugar

imports

  signatures/java/-
  
  java/classes/trans/desugar
  java/interfaces/trans/desugar
  java/statements/trans/desugar
  java/expressions/trans/desugar
  java/lexical/trans/desugar
  java/packages/trans/desugar

strategies

  desugar-all-singleton-appl = topdown(try(desugar-singleton-appl)); topdown(try(flatten-list))
  desugar-all                = topdown(try(desugar)); topdown(try(flatten-list))

rules

  desugar-singleton-appl:
    Some(t) -> t
