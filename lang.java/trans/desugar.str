module desugar

imports

  signatures/-
  
  spec/classes/trans/desugar
  spec/interfaces/trans/desugar
  spec/statements/trans/desugar
  spec/expressions/trans/desugar
  spec/lexical/trans/desugar
  spec/packages/trans/desugar

strategies

  desugar-all-singleton-appl = topdown(try(desugar-singleton-appl)); topdown(try(flatten-list))
  desugar-all                = topdown(try(desugar)); topdown(try(flatten-list))

rules

  desugar-singleton-appl:
    Some(t) -> t
